<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if @cart_empty %>
    <%= render 'empty' %>
  <% else %>
    <%= render 'with_products' %>
  <% end %>
    
</section>
